{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "is-it-shabbat-core": "^1.0.7",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-redux": "^6.0.1",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn build && gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "homepage": "https://isitshabbat.org",
  "peerDependencies": {
    "luxon": "^1.12.0",
    "shabbat-logic": "^1.0.0"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
